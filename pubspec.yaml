name: table_bison
description: A BSON based data storage system.
version: 0.0.5
repository: https://github.com/LiveUser/table_bison_dart

environment:
  sdk: ^3.13.1

# Add regular dependencies here.
dependencies:
  bson: ^5.0.8

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
